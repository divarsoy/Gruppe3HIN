using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using SysUt14Gr03.Models;

namespace SysUt14Gr03.Classes
{
    public static class Varsel
    {
        static public void SendVarsel(int bruker_id, int varsel_id)
        {
            // Sjekker brukerpreferanser
            /*
            if (){

            }
            string epost = "";
            string message = "";
            string subject = "";
            // Sender e-post
            sendEmail sendEmail = new sendEmail();
            sendEmail.sendEpost(epost, message, subject, null, null, null);

            // Sender intern varsel
             * */
        }
    }
}